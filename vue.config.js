// 这个文件的名字是固定的
// 本来没有这个文件, 不影响任何功能
// 但是如果你手动创建了这个文件
// 里面的配置自动生效

module.exports = {
    // 这里是脚手架的配置,
    // 其中如果想修改 webpack 配置
    configureWebpack: {
        // 声明在 configureWebpack 属性对象里面即可
        // 如果没有这个需要, 这个文件可以不存在
    }
}